services:
  - type: web
    name: searxng
    runtime: docker
    region: oregon
    plan: free
    dockerfile: ./Dockerfile
    port: 10000
    envVars:
      - key: SEARXNG_BASE_URL
        scope: RUN
      - key: SEARXNG_SECRET_KEY
        scope: RUN
